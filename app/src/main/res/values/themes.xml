<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TempNavigation" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">true</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TempNavigation" parent="Base.Theme.TempNavigation" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="BottomNavigationView.Borderless" parent="Widget.Design.BottomNavigationView">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="theme">@style/Widget.Material3.BottomNavigationView</item>
    </style>

</resources>